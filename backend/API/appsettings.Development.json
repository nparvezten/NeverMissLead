{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.EntityFrameworkCore": "Information"
      }
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5433;Database=nevermisslead;Username=postgres;Password=postgres;Include Error Detail=true"
  },
  "RagService": {
    "BaseUrl": "http://localhost:8000"
  },
  // ── AI Provider config (dev overrides) ────────────────────────────────────
  // Real API keys go in appsettings.Local.json (git-ignored), never here.
  "AiProviders": {
    "LlmProvider": "none"
  }
}
